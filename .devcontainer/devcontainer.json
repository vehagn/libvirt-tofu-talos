{
  "name": "libvirt-tofu-talos",
  "build": {
    "dockerfile": "Containerfile"
  },
  "remoteUser": "dev",
  "mounts": [
    {
      "source": "${localEnv:SSH_AUTH_SOCK}",
      "target": "/ssh-agent",
      "type": "bind"
    }
  ],
  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  }
}
