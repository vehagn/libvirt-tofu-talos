HYPERVISOR_HOST=192.168.1.100
HYPERVISOR_USER=root
VM_BRIDGE_INTERFACE=br0
VM_USER_PASSWORD=ubuntu

# Talos cluster (used by tofu/talos). Nodes use DHCP; VIP is the static
# cluster endpoint and must be a free address on the bridge subnet.
TALOS_VIP=192.168.1.99
